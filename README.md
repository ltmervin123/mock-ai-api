```
This project use pnpm as package manager
if pnpm is not installed locally use this script to

# Install pnpm locally
npm install -g pnpm@latest-11

# Run package
pnpm install

# Run app locally
pnpm run dev
```

```
open http://localhost:3000
```
